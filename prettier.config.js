/** @type {import('prettier').Config} */
const config = {
    arrowParens: 'avoid',
    experimentalTernaries: true,
    printWidth: 95,
    tabWidth: 4,
    singleQuote: true,
};

export default config;
